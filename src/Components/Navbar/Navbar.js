import React from "react";
import './navbar.css';

const Navbar = (props) => {
    return(
        <div className="navbar">
            <span className="logo">Mon chat</span>
        </div>
    )
}

export default Navbar
